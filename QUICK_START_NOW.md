# 🚀 立即开始 - Material 3 演示

你的 Qt 已安装，现在只需 3 步！

---

## 方法 1：使用 Qt Creator（最简单）⭐

### 步骤 1：打开 Qt Creator

在开始菜单找到并打开 **Qt Creator**

### 步骤 2：打开项目

1. 点击 **文件** → **打开文件或项目**
2. 浏览到：`D:\编程项目\freeWindows\demos\Material3Demo`
3. 选择 **CMakeLists.txt**
4. 点击 **打开**

### 步骤 3：运行

1. 点击左下角的 **配置项目** 按钮
2. 点击左下角的 **绿色播放按钮** ▶️
3. 等待编译完成
4. 看到你的第一个 Material 3 窗口！🎉

---

## 方法 2：使用命令行

### 如果你喜欢命令行

```powershell
# 进入项目目录
cd demos\Material3Demo

# 配置（使用你的 Qt 路径）
cmake -B build -G "MinGW Makefiles" -DCMAKE_PREFIX_PATH=D:\QT\6.9.3\mingw_64

# 构建
cmake --build build

# 运行
.\build\Material3Demo.exe
```

---

## 方法 3：自动构建脚本

```powershell
# 运行自动构建脚本
.\scripts\build-material3-demo.ps1
```

这个脚本会：
- ✅ 自动找到 Qt
- ✅ 自动配置项目
- ✅ 自动构建
- ✅ 询问是否运行

---

## 你会看到什么？

一个漂亮的窗口，包含：

```
╭─────────────────────────────────╮
│ Material 3 Demo            × │  ← 可拖动标题栏
├─────────────────────────────────┤
│                                 │
│  Material 3 演示                │  ← 白色标题
│  使用 Qt 实现的现代化界面       │  ← 副标题
│                                 │
│  ╭─────────────────────╮       │
│  │   主要按钮          │       │  ← 紫色按钮
│  ╰─────────────────────╯       │
│                                 │
│  ╭─────────────────────╮       │
│  │   次要按钮          │       │  ← 灰色按钮
│  ╰─────────────────────╯       │
│                                 │
│  ╭─────────────────────╮       │
│  │   第三按钮          │       │  ← 粉色按钮
│  ╰─────────────────────╯       │
│                                 │
╰─────────────────────────────────╯

特性：
✅ 圆角窗口
✅ 渐变背景（紫色）
✅ 带阴影的按钮
✅ 点击动画
✅ 可拖动窗口
```

---

## 如果遇到问题

### 问题 1：找不到 Qt

**解决方案**：
```powershell
# 手动指定 Qt 路径
cmake -B build -DCMAKE_PREFIX_PATH=D:\QT\6.9.3\mingw_64
```

### 问题 2：找不到 CMake

**解决方案**：
- 使用 Qt Creator（推荐）
- 或安装 CMake：https://cmake.org/download/

### 问题 3：编译错误

**解决方案**：
1. 确保 Qt 版本 ≥ 5.15
2. 使用 Qt Creator 打开项目
3. 让 Qt Creator 自动配置

---

## 下一步

### 修改代码

编辑 `demos\Material3Demo\modernwindow.cpp`：

```cpp
// 修改标题
QLabel* title = new QLabel("你的标题");

// 修改颜色
gradient.setColorAt(0, QColor("#FF0000"));  // 改成红色

// 添加更多按钮
contentLayout->addWidget(createMaterial3Button("新按钮", "#00FF00"));
```

### 学习更多

- 📄 查看代码：`demos\Material3Demo\`
- 📚 阅读文档：`docs\qt-material3-quick-start.md`
- 🎨 Material 3 设计：`modern-ui-implementation-guide.md`

---

## 项目文件

```
demos\Material3Demo\
├── CMakeLists.txt      # CMake 配置
├── main.cpp            # 主程序
├── modernwindow.h      # 窗口头文件
├── modernwindow.cpp    # 窗口实现
└── README.md           # 说明文档
```

---

## 常用命令

```powershell
# 重新创建项目
.\scripts\create-material3-demo.ps1

# 构建项目
.\scripts\build-material3-demo.ps1

# 清理构建
Remove-Item demos\Material3Demo\build -Recurse -Force

# 重新构建
cmake --build demos\Material3Demo\build --clean-first
```

---

## 成功标志

当你看到：
- ✅ 一个圆角窗口
- ✅ 紫色渐变背景
- ✅ 三个带阴影的按钮
- ✅ 点击按钮有动画
- ✅ 可以拖动窗口

**恭喜！你已经成功创建了第一个 Material 3 应用！** 🎉

---

## 现在就开始！

**推荐方式**：

1. 打开 **Qt Creator**
2. 打开 `demos\Material3Demo\CMakeLists.txt`
3. 点击 **运行** ▶️

**只需 3 步，30 秒！**

---

**创建时间**：2025-10-30  
**难度**：⭐☆☆☆☆ (非常简单)  
**时间**：< 1 分钟

